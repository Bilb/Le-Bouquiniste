type User {
    id: ID! @id
    email: String! @unique
    fullname: String!
    password: String!
    samples: [Sample!]!
}

type Sample {
    id: ID! @id
    isbn: String!
    title: String!
    author: String
}

